# examples/
> L2 | 父级: /agents/AGENTS.md

成员清单
jp-ads-role.fixture.md: JP Ads seed fixture，验证 Ads role + Jetpartner overlay 能按 shared schema 表达。
caylent-event-role.fixture.md: Caylent Event seed fixture，验证 Event role + Caylent overlay 能按 shared schema 表达。

边界
examples/ contains validation fixtures only. It is not runtime config and does not grant tool access.

[PROTOCOL]: 变更时更新此头部，然后检查 AGENTS.md
