# deltas/
> L2 | 父级: /agents/state/AGENTS.md

Verified GEB deltas promoted from run readbacks for the __NAME_TITLE__ agent.
Each delta names evidence, owner, target artifact, and review horizon.

成员清单
.gitkeep: Keeps the verified delta directory present until the first `*.delta.yaml` exists.

边界
deltas/ contains reviewed semantic changes only. Proposed learning may live here
only when evidence and route are explicit; otherwise stop at readback.

[PROTOCOL]: 变更时更新此头部，然后检查 AGENTS.md
