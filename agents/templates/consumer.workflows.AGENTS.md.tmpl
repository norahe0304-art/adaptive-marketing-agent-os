# workflows/
> L2 | 父级: /agents/AGENTS.md

Playbook workflow contracts for the __NAME_TITLE__ agent. Workflows bind role
surfaces to overlay bindings, approval gates, evidence packets, readbacks, and
proactive GEB learning routes. They are path-portable contracts, not runtime logs.

成员清单
__NAME__-__PLAYBOOK__.workflow.md: __PLAYBOOK_TITLE__ playbook workflow. Reads evidence, drafts reviewable proposals, keeps default mode `propose`, and requires reusable-learning readback.

边界
workflows/ owns process shape. Role files own reusable __DOMAIN_TITLE__ principles;
overlays own __TENANT__ tenant truth; state/ owns run readbacks and reviewed memory.

[PROTOCOL]: 变更时更新此头部，然后检查 AGENTS.md
